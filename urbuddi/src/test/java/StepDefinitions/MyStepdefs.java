package StepDefinitions;

public class MyStepdefs {
    public MyStepdefs() {
        Given("^Urbuddi is launched \"([^\"]*)\"$", (String arg0) -> {
            driver
        });
        When("^user enter \"([^\"]*)\" and \"([^\"]*)\"$", (String arg0, String arg1) -> {
        });
        Then("^user login succesfully$", () -> {
        });
    }
}
